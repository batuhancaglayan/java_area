name=PropertiesConfig
property.filename = logs
appenders = rolling, console

appender.console.type = Console
appender.console.name = STDOUT
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = [%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n

appender.rolling.type = RollingFile
appender.rolling.name = AppLogs
appender.rolling.fileName=${filename}/applogs.log
appender.rolling.layout.type=PatternLayout
appender.rolling.layout.pattern=[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n
appender.rolling.filePattern= ${basePath}app_%d{yyyyMMdd}.log.gz
appender.rolling.policies.type = Policies

#appender.spring.log.type = File
#appender.spring.log.name = SpringLog
#appender.spring.log.fileName=${filename}/springlogs.log
#appender.spring.log.layout.type=PatternLayout
#appender.spring.log.layout.pattern=[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n

#logger.file.additivity = false

rootLogger.level = debug
rootLogger.appenderRefs = stdout
rootLogger.appenderRef.stdout.ref = STDOUT

#loggers=rolling
#
#logger.rolling.name=AppLogs
#logger.rolling.level = debug
#logger.rolling.appenderRefs = rolling
#logger.rolling.appenderRef.rolling.ref = AppLogs
